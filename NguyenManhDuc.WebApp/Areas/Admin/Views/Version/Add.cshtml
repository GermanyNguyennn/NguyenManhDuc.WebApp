@using NguyenManhDuc.WebApp.Models
@model VersionModel
@{
    ViewData["Title"] = "Thêm Phiên Bản";
}
<div class="">
    <form asp-action="Add" enctype="multipart/form-data">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-group">
            <label>Phiên Bản</label>
            <input asp-for="Version" class="form-control" />
            <span asp-validation-for="Version" class="text-danger" />
        </div>

        <div class="form-group">
            <button class="btn btn-warning btn-sm">Thêm</button>
            <a asp-action="Index" asp-controller="Version" class="btn btn-warning btn-sm">Quay Lại</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
